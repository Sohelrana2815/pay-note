In my InvoiceForm.tsx file codes.I used the items and this file has invoice values which infer the ZOD from my ZOD schema. And you have already know that item has array of object and it has item name, item quantity, item price. So in this case, I used in my invoice file.tsx file like this.

  items: [
        {
          itemName: "",
          itemQuantity: 1,
          itemPrice: "",
        },
      ],


InvoiceForm.tsx : type InvoiceValues = z.infer<typeof invoiceSchema>;
  const form = useForm<InvoiceValues>({
    defaultValues: {
      type: "INVOICE" as const,
      businessName: "",
      logoUrl: "",
      invoiceNumber: "",
      date: new Date(),
      clientName: "",
      clientEmail: "",
      clientPhone: "+8801",
      items: [
        {
          itemName: "",
          itemQuantity: 1,
          itemPrice: "",
        },
      ],
      paymentMethod: "",
      notes: "",
    },
    resolver: zodResolver(invoiceSchema),
    mode: "onChange",
  });

  const {
    fields: items,
    append: addItem,
    remove: removeItem,
  } = useFieldArray({
    control: form.control,
    name: "items",
  });

  const onSubmit = (data: InvoiceValues) => {
    console.log("Form Data:", data);
  };



current moment, I just have item name. So clicking add item button only add one field, which is just item name. But you have already know that items is an array of objects and inside this array of object, I have three properties, item name, item quantity, and price. So in this case, when I click add item button, I want to also add two fields, which is item quantity, item name. So how can I do this?